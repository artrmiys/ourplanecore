<Window x:Class="SmartTakeoffs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmartTakeoffs" Height="780" Width="1280"
        MinHeight="600" MinWidth="900"
        Background="#2B2B2B">
    <DockPanel x:Name="RootDock">

        <!-- ── Toolbar ─────────────────────────────────────────────────────── -->
        <ToolBar x:Name="MainToolBar" DockPanel.Dock="Top" Background="#F0F0F0">

            <Button x:Name="BtnOpen" Content="📂 Open Job"
                    ToolTip="Open a SmartTakeoffs job folder (Ctrl+O)"
                    Click="BtnOpen_Click" Style="{StaticResource ToolBtn}"/>
            <Button x:Name="BtnOpenJobsFolder" Content="Jobs"
                    ToolTip="Open a folder containing multiple jobs"
                    Click="BtnOpenJobsFolder_Click" Style="{StaticResource ToolBtn}"/>
            <Button x:Name="BtnNewJob" Content="＋ New Job"
                    ToolTip="Create a new job folder"
                    Click="BtnNewJob_Click" Style="{StaticResource ToolBtn}"/>

            <Separator/>
            <TextBlock x:Name="TxtJobName" VerticalAlignment="Center"
                       Text="— no job —" FontSize="11" Margin="4,0"
                       FontStyle="Italic"/>

            <Separator/>

            <!-- Drawing tools -->
            <Button x:Name="BtnPan"   Content="Pan ✋"  Click="BtnTool_Click" Tag="pan"
                    ToolTip="Pan (V)"   Style="{StaticResource ToolBtnActive}"/>
            <Button x:Name="BtnScale" Content="Scale 📐" Click="BtnTool_Click" Tag="scale"
                    ToolTip="Set scale (S)" Style="{StaticResource ToolBtn}"/>
            <Button x:Name="BtnPoint" Content="Point ●"  Click="BtnTool_Click" Tag="point"
                    ToolTip="Count point (P)" Style="{StaticResource ToolBtn}"/>
            <Button x:Name="BtnLine"  Content="Line ╱"   Click="BtnTool_Click" Tag="line"
                    ToolTip="Measure line (L)" Style="{StaticResource ToolBtn}"/>
            <Button x:Name="BtnArea"  Content="Area ▭"   Click="BtnTool_Click" Tag="area"
                    ToolTip="Measure area (A)" Style="{StaticResource ToolBtn}"/>

            <Separator/>

            <!-- Zoom controls -->
            <Button Content="Fit"  Click="BtnFit_Click"    ToolTip="Fit page (F)"
                    Style="{StaticResource ToolBtn}"/>
            <Button Content="+"    Click="BtnZoomIn_Click"  ToolTip="Zoom in (Ctrl++)"
                    MinWidth="28" Style="{StaticResource ToolBtn}"/>
            <Button Content="−"    Click="BtnZoomOut_Click" ToolTip="Zoom out (Ctrl+−)"
                    MinWidth="28" Style="{StaticResource ToolBtn}"/>

            <Separator/>

            <!-- Scale ratio -->
            <Label Content="1:" VerticalAlignment="Center" Padding="0"/>
            <TextBox x:Name="TxtScaleRatio" Width="55" VerticalAlignment="Center"
                     Text="100" ToolTip="Scale ratio (1:N) — press Enter to apply"
                     KeyDown="TxtScaleRatio_KeyDown"/>
            <Button Content="Set" Click="BtnSetScale_Click"
                    ToolTip="Apply scale" Style="{StaticResource ToolBtn}"/>
            <Button Content="▾ Presets" Click="BtnScalePresets_Click"
                    ToolTip="Common scale presets" Style="{StaticResource ToolBtn}"/>
            <TextBlock x:Name="TxtScaleInfo" VerticalAlignment="Center"
                       Foreground="#0055CC" FontSize="11" Margin="4,0"/>

            <Separator/>
            <ToggleButton x:Name="BtnImperial" Content="ft"
                          ToolTip="Toggle imperial units (ft / sf / ea)"
                          Checked="BtnImperial_Checked" Unchecked="BtnImperial_Unchecked"
                          Padding="6,2" MinWidth="32"/>
            <Button x:Name="BtnViewportBg" Content="BG" Click="BtnViewportBg_Click"
                    ToolTip="Viewport background" Style="{StaticResource ToolBtn}"/>
            <ToggleButton x:Name="BtnDarkTheme" Content="Dark"
                          ToolTip="Toggle dark/light UI"
                          Checked="BtnDarkTheme_Checked" Unchecked="BtnDarkTheme_Unchecked"
                          Padding="6,2" MinWidth="48"/>

            <Separator/>
            <TextBlock x:Name="TxtTool" VerticalAlignment="Center"
                       Text="  Tool: Pan" FontSize="11"/>
        </ToolBar>

        <!-- ── Status bar ──────────────────────────────────────────────────── -->
        <StatusBar x:Name="MainStatusBar" DockPanel.Dock="Bottom" Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource ControlBorderBrush}" BorderThickness="0,0,0,1">
            <StatusBarItem>
                <TextBlock x:Name="TxtStatusJob" Text="No job"
                           FontWeight="SemiBold" Margin="0,0,4,0"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem MinWidth="150">
                <TextBlock x:Name="TxtStatusPage" Text="—"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="TxtStatus"
                           Text="Ready. Open or create a job to start."/>
            </StatusBarItem>
        </StatusBar>

        <!-- ── Main area: 3-column content + AI Inbox ──────────────────────── -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" MinHeight="280"/>
                <RowDefinition x:Name="InboxSplitterRow" Height="4"/>
                <RowDefinition x:Name="InboxRow" Height="170" MinHeight="30"/>
            </Grid.RowDefinitions>

            <!-- ── Row 0: 3-column layout ───────────────────────────────── -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" MinWidth="120"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"   MinWidth="400"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="220" MinWidth="120"/>
                </Grid.ColumnDefinitions>

                <!-- Left panel: Pages -->
                <DockPanel x:Name="PagesPanel" Grid.Column="0" Background="#F5F5F5">
                    <Border DockPanel.Dock="Top" Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource ControlBorderBrush}" BorderThickness="0,0,0,1" Padding="6,5">
                        <TextBlock Text="Pages" FontWeight="Bold" FontSize="12"/>
                    </Border>
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="4">
                        <Button Content="Import PDF" Click="BtnImport_Click"
                                Padding="5,3" ToolTip="Add PDF pages to current job"/>
                    </StackPanel>
                    <!-- PDF Layers sub-panel -->
                    <DockPanel DockPanel.Dock="Bottom">
                        <Border DockPanel.Dock="Top" Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource ControlBorderBrush}" BorderThickness="0,0,0,1" Padding="6,3">
                            <TextBlock Text="PDF Layers" FontWeight="Bold" FontSize="11"/>
                        </Border>
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="4,2">
                            <Button x:Name="BtnLayersOn"      Content="All On"
                                    Click="BtnLayersOn_Click"
                                    Padding="3,1" Margin="0,0,4,0" FontSize="10"/>
                            <Button x:Name="BtnLayersOff"     Content="All Off"
                                    Click="BtnLayersOff_Click"
                                    Padding="3,1" FontSize="10"/>
                            <Button x:Name="BtnLayersClearHi" Content="Clear Hi"
                                    Click="BtnLayersClearHi_Click"
                                    Padding="3,1" Margin="4,0,0,0" FontSize="10"/>
                        </StackPanel>
                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="130">
                            <StackPanel x:Name="LayersPanel" Margin="4,0"/>
                        </ScrollViewer>
                    </DockPanel>
                    <TreeView x:Name="PagesTree"
                              SelectedItemChanged="PagesTree_SelectedItemChanged"
                              Margin="2"/>
                </DockPanel>

                <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                              Background="#CCC" ResizeBehavior="PreviousAndNext"/>

                <!-- Centre: PDF viewport -->
                <Grid x:Name="ViewportHost" Grid.Column="2" Background="#FFFFFF"/>

                <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch"
                              Background="#CCC" ResizeBehavior="PreviousAndNext"/>

                <!-- Right panel: Takeoffs -->
                <DockPanel x:Name="TakeoffsPanel" Grid.Column="4" Background="#F5F5F5">
                    <Border DockPanel.Dock="Top" Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource ControlBorderBrush}" BorderThickness="0,0,0,1" Padding="6,5">
                        <TextBlock Text="Takeoffs" FontWeight="Bold" FontSize="12"/>
                    </Border>
                    <StackPanel DockPanel.Dock="Bottom" Margin="4,4">
                        <TextBlock x:Name="TxtTotal" Text="Total: —"
                                   FontWeight="Bold" FontSize="12" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Button Content="Save"       Click="BtnSave_Click"
                                    Padding="5,3" Margin="0,0,4,0"/>
                            <Button Content="Export CSV" Click="BtnExportCsv_Click"
                                    Padding="5,3"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="New Folder" Click="BtnNewTakeoffFolder_Click"
                                    Padding="5,3" Margin="0,0,4,0"/>
                            <Button Content="New Item"   Click="BtnNewItem_Click"
                                    Padding="5,3"/>
                        </StackPanel>
                    </StackPanel>
                    <TreeView x:Name="TakeoffsTree"
                              SelectedItemChanged="TakeoffsTree_SelectedItemChanged"
                              Margin="2"/>
                </DockPanel>
            </Grid>

            <!-- ── Row 1: Inbox splitter ─────────────────────────────────── -->
            <GridSplitter x:Name="InboxSplitter" Grid.Row="1" Height="4"
                          HorizontalAlignment="Stretch" Background="#AAAAAA"
                          ResizeBehavior="PreviousAndNext"/>

            <!-- ── Row 2: AI Inbox ────────────────────────────────────────── -->
            <Grid x:Name="InboxPanel" Grid.Row="2" Background="White">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Inbox header bar — same background as toolbar/statusbar, set via ApplyTheme -->
                <Border x:Name="InboxHeaderBorder" Grid.Row="0"
                        BorderBrush="{DynamicResource ControlBorderBrush}"
                        BorderThickness="0,1,0,0">
                    <DockPanel Margin="8,0">
                        <!-- Toggle collapse -->
                        <Button x:Name="BtnToggleInbox" DockPanel.Dock="Right"
                                Click="BtnToggleInbox_Click"
                                Background="Transparent" BorderThickness="0"
                                Padding="6,0" MinWidth="28" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtInboxToggle" Text="▾" FontSize="13"/>
                        </Button>
                        <!-- Add observation -->
                        <Button x:Name="BtnInboxAdd" Content="+ Add"
                                DockPanel.Dock="Right"
                                Click="BtnAddObservation_Click"
                                Style="{StaticResource ToolBtn}"
                                Padding="7,2" Margin="4,3" FontSize="11"
                                VerticalAlignment="Center"/>
                        <!-- Title -->
                        <TextBlock x:Name="TxtInboxTitle" Text="AI Inbox"
                                   FontWeight="Bold" VerticalAlignment="Center"
                                   FontSize="12"/>
                        <!-- Count badge — subtle accent pill -->
                        <Border x:Name="InboxBadge" CornerRadius="8"
                                Background="{DynamicResource ControlActiveBackgroundBrush}"
                                Padding="5,1" Margin="8,0,0,0"
                                VerticalAlignment="Center" Visibility="Collapsed">
                            <TextBlock x:Name="TxtInboxCount" Text="0"
                                       Foreground="{DynamicResource ControlActiveForegroundBrush}"
                                       FontSize="10" FontWeight="Bold"/>
                        </Border>
                    </DockPanel>
                </Border>

                <!-- Observations list -->
                <ListView x:Name="ObservationsListView" Grid.Row="1"
                          Background="White" BorderThickness="0" FontSize="11"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          VirtualizingPanel.IsVirtualizing="True">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Type" Width="105">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="3" Padding="5,1"
                                                Background="{Binding BadgeColor}"
                                                HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding TypeShort}"
                                                       Foreground="White" FontSize="10"/>
                                        </Border>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Page" Width="80"
                                            DisplayMemberBinding="{Binding Page}"/>
                            <GridViewColumn Header="Observation" Width="500"
                                            DisplayMemberBinding="{Binding TextPreview}"/>
                            <GridViewColumn Header="Time" Width="130"
                                            DisplayMemberBinding="{Binding TimeDisplay}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>

        </Grid>

    </DockPanel>
</Window>
